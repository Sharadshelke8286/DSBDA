import pandas as pd
import seaborn as sns
import matplotlib.pyplot as plt

df = sns.load_dataset('titanic')

df.head()

df.info()

df['age'].fillna(df['age'].median(), inplace=True)

df.isnull().sum()

plt.figure(figsize=(8,5))
sns.boxplot(x=df['age'])
plt.title("Boxplot of Age")
plt.xlabel("Age")
plt.show()

plt.figure(figsize=(10,6))
sns.boxplot(x='sex', y='age', hue='survived', data=df)
plt.title("Age vs Gender vs Survival")
plt.xlabel("Gender")
plt.ylabel("Age")
plt.show()

Q1 = df['age'].quantile(0.25)
Q3 = df['age'].quantile(0.75)
IQR = Q3 - Q1

lower_bound = Q1 - 1.5 * IQR
upper_bound = Q3 + 1.5 * IQR

outliers = df[(df['age'] < lower_bound) | (df['age'] > upper_bound)]

lower_bound
upper_bound
outliers[['age']]

print("Observations:")
print("Most passengers are between 20–40 years")
print("Median age is around 28–30")
print("Some high-age values are outliers")
print("Females have higher survival rates")
print("Children have better survival chances")

print("Inferences:")
print("Women and children were prioritized")
print("Gender and age influenced survival")
print("Adult males had lower survival chances")
print("Age distribution is slightly right-skewed")